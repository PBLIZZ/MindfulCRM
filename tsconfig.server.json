{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    // server code doesn't have JSX
    "jsx": "preserve"
  },
  "include": [
    "server",
    "shared",
    "types"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "client"
  ]
}